<MahApps:MetroWindow x:Class="TPGestionFilm.Views.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TPGestionFilm.Views"
        xmlns:MahApps="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        Title="Shell" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="Home" Command="{Binding NavigateHome}"/>
            <MenuItem Header="Ajouter" Command="{Binding NavigateMovie}"/>
            <MenuItem Header="Parametres" Command="{Binding NavigateSettings}"/>
        </Menu>

        <ContentPresenter Grid.Row="1" Content="{Binding DisplayedView}" />
    </Grid>
</MahApps:MetroWindow>
